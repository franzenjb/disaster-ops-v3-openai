# Environment Configuration for Disaster-Ops-V3
# Copy this file to .env.local and fill in your actual values

# Google Maps API Key (Required for address autocomplete)
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Maps JavaScript API and Places API
# 4. Create API key and restrict to your domain
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE

# Supabase Configuration (Required for authentication and real-time features)
# 1. Create account at https://supabase.com
# 2. Create new project
# 3. Run the SQL schema from supabase-setup.sql in SQL Editor
# 4. Get URL and anon key from Settings > API
NEXT_PUBLIC_SUPABASE_URL=YOUR_SUPABASE_URL_HERE
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY_HERE

# Development Note:
# - Google Maps API key enables address autocomplete in facility creation
# - Supabase credentials enable real-time collaboration and authentication
# - The app will run in demo mode without these, but with limited functionality